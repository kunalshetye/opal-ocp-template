{
  "name": "{{APP_ID}}",
  "version": "1.0.0-dev.1",
  "description": "{{APP_DESCRIPTION}}",
  "main": "dist/index.js",
  "scripts": {
    "clean": "rimraf dist",
    "compile": "tsc",
    "copy-config": "copyfiles -u 0 app.yml \"forms/**/*\" \"assets/**/*\" dist/",
    "build": "yarn clean && yarn compile && yarn copy-config",
    "lint": "echo 'No linter configured'",
    "test": "echo 'No tests configured'",
    "validate": "yarn build && cross-env LOG_LEVEL=never node node_modules/@zaiusinc/app-sdk/dist/app/validation/runValidation.js"
  },
  "dependencies": {
    "@optimizely-opal/opal-tool-ocp-sdk": "1.0.0",
    "@zaiusinc/app-sdk": "3.2.0",
    "@zaiusinc/node-sdk": "3.0.0"
  },
  "devDependencies": {
    "@types/node": "^20.0.0",
    "copyfiles": "^2.4.1",
    "cross-env": "^7.0.3",
    "rimraf": "^5.0.0",
    "typescript": "^5.0.0"
  },
  "engines": {
    "node": ">=22.0.0"
  },
  "resolutions": {
    "grpc-boom": "3.0.11"
  }
}
